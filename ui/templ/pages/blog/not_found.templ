package blog

import (
	"github.com/jonathanschwarzhaupt/home-blog/ui/templ/layout"
)

templ NotFound() {
	@layout.Base("Not Found", "") {
		<div class="py-12 text-center">
			<h1 class="text-2xl font-semibold tracking-tight">Page not found</h1>
			<p class="mt-2 text-muted-foreground">There's nothing here</p>
			<p class="mt-2 text-muted-foreground">The link might be old, the page never existed, or the beaver chew through the cable.</p>
			<div class="mt-6">
				<a href="/" class="text-primary hover:underline">Back to Home</a>
			</div>
		</div>
	}
}
